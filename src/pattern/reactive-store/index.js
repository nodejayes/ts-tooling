/**
 * a Immutable Store dealing with Subjects to be reactive
 *
 * @module pattern/reactive-store
 */
const {ReactiveStore, SafeBehaviorSubject} = require('./store/reactive.store');

module.exports = {ReactiveStore, SafeBehaviorSubject};
