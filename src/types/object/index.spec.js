const {assert} = require('chai');
const {ObjectFactory} = require('./index');

describe('Type Object Export Tests', () => {
    it('ObjectFactory', () => {
        assert.isDefined(ObjectFactory);
    });
});
